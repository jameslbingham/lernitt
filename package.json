{
  "name": "lernitt-root",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "client:dev:mock": "cd client && npm run dev:mock",
    "client:dev:live": "cd client && npm run dev:live",
    "client:build": "cd client && npm run build",
    "client:preview": "cd client && npm run preview",
    "server:start": "cd server && npm start",
    "server:cleanup:recordings": "cd server && npm run cleanup:recordings"
  },
  "dependencies": {},
  "devDependencies": {}
}
